@model ProjectSample.Areas.Account.Models.Register.RegisterFields
    
@{
    ViewBag.Title = "Register";
}

<h2>Register</h2>

@using (Html.BeginForm("Index", "Register", new {area = "Account"}, FormMethod.Post, new {@class = "form-horizontal"}))
{
    @Html.EditorForModel()
    <div class="form-group">
        <div class="col-sm-offset-4 col-md-offset-4 col-lg-offset-4 col-sm-8 col-md-8 col-lg-8">
            <button type="submit" class="btn btn-success">Register</button>
        </div>
    </div>
}


<h3>@ViewBag.EntityType details</h3>

<div class="row">
    <div class="col-md-8">
        <p>
            @{ Html.RenderPartial("DetailsActions");}
        </p>
    </div>
</div>

@Html.DisplayForModel()

@section scripts {
    @{Html.RenderPartial("DetailsScripts");}
}